{
  "name": "react-parcel-ssr",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "babel-register": "^6.26.0",
    "classnames": "^2.2.6",
    "express": "^4.16.3",
    "lodash": "^4.17.10",
    "prop-types": "^15.6.2",
    "react": "^16.4.1",
    "react-cookie": "^2.2.0",
    "react-dom": "^16.4.1",
    "react-router": "^4.3.1",
    "react-router-dom": "^4.3.1",
    "universal-cookie-express": "^2.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^8.6.5",
    "babel-cli": "^6.26.0",
    "babel-plugin-css-modules-transform": "^1.6.1",
    "babel-plugin-root-import": "^6.1.0",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "concurrently": "^3.6.0",
    "parcel": "^1.9.4",
    "parcel-bundler": "^1.9.4",
    "postcss-modules": "^1.1.0",
    "postcss-nested": "^3.0.0"
  },
  "scripts": {
    "clear": "rm -rf .tmp .cache",
    "clearbuild": "rm -rf build",

    "dev": "yarn clear && BABEL_ENV=client parcel src/index.html --out-dir .tmp --public-url /static/",
    "devserver": "BABEL_ENV=server babel-node server/app.js",
    "ssr": "concurrently 'yarn dev' 'yarn devserver'",

    "buildclient": "BABEL_ENV=client parcel build src/index.html --no-cache --public-url /static -d build/static --no-source-maps",
    "buildserver": "BABEL_ENV=client parcel build server/app.js --no-cache -d build -t node --no-source-maps",
    "build": "yarn clearbuild && concurrently 'yarn buildclient' 'yarn buildserver'",

    "serve": "NODE_ENV=production node build/app.js"
  }
}
